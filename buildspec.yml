version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo ☑️ Install 단계 시작!
      - export MAVEN_HOME=$CODEBUILD_SRC_DIR/Team5/apache-maven-3.9.5
      - export PATH=$MAVEN_HOME/bin:$PATH
      - mvn clean install

  build:
    commands:
      - mvn package -DskipTests

artifacts:
  files:
    - target/project1.war
  discard-paths: yes

